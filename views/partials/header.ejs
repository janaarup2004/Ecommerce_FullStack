<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <nav class="w-full flex justify-between px-5 py-3 items-center">
        <h2 class="text-xl">Art's Craft</h2>
        <div class="flex gap-5 items-center">
            <% var loggedin=typeof loggedin !== "undefined" ? loggedin :true %>
            <% if(loggedin){ %>
                <a href="/shop">Shop</a>
                <a href="/cart">Cart</a>
                <a href="/myaccount">My Account</a>
                <a class="text-red-600" href="/users/logout">Logout</a>
            <% } %>
            <% if (typeof currentPath !== 'undefined' && currentPath === '/') { %>
                <a href="/owner-login" class="ml-6 px-5 py-2 bg-gradient-to-r from-purple-600 to-blue-600 text-white font-bold rounded-full shadow hover:from-purple-700 hover:to-blue-700 transition-all">Owner Login</a>
            <% } %>
        </div>
    </nav>
</body>
</html>

<!-- add an feature owner login at upper right position , and it was show in only home("/")  page -->