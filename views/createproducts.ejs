<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
<body>

<% if(success.length>0){ %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= success %>
            </span>
        </div>
    <% } %>


<div class="min-h-screen flex flex-col bg-gradient-to-br from-blue-100 to-blue-200">
<div class="w-full flex justify-end items-center p-4">
  <a href="/admin/logout" class="inline-flex items-center px-6 py-2 bg-gradient-to-r from-red-500 to-red-700 text-white font-bold rounded-full shadow-lg hover:from-red-600 hover:to-red-800 transition-all duration-200 border-2 border-red-600 hover:scale-105">
    <svg class="h-5 w-5 mr-2" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7" /></svg>
    Admin Logout
  </a>
</div>
    <div class="container mx-auto px-4 py-12 flex flex-col md:flex-row gap-8 flex-grow">
        <!-- Sidebar -->
        <aside class="w-full md:w-1/4 bg-white rounded-lg shadow-md p-6 flex flex-col items-start mb-8 md:mb-0">
            <nav class="w-full">
                <a class="block w-full mb-3 px-4 py-2 rounded hover:bg-blue-100 text-blue-700 font-medium transition" href="#">All Products</a>
                <a class="block w-full mb-3 px-4 py-2 rounded hover:bg-blue-100 text-blue-700 font-medium transition" href="#">Create new product</a>
            </nav>
        </aside>
        <!-- Main Content -->
        <main class="w-full md:w-3/4 bg-white rounded-lg shadow-md p-8">
            <h2 class="text-2xl font-bold mb-6 text-blue-700 flex items-center">
                <svg class="h-7 w-7 mr-2 text-blue-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" /></svg>
                Create New Product
            </h2>
            <form autocomplete="off" action="/products/create" method="post" enctype="multipart/form-data" class="space-y-8">
                <div>
                    <h3 class="text-lg font-semibold mb-4 text-gray-700">Product Details</h3>
                    <div class="mb-6">
                        <label class="block mb-2 font-medium text-gray-700">Product Image</label>
                        <input name="image" type="file" class="block w-full py-2 px-4 rounded border border-gray-300 bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400" value="Select File">
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <input name="name" type="text" placeholder="Product Name"
                            class="border p-3 rounded w-full bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400" required>
                        <input name="price" type="text" placeholder="Product Price"
                            class="border p-3 rounded w-full bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400" required>
                        <input name="discount" type="text" placeholder="Discount Price"
                            class="border p-3 rounded w-full bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400">
                    </div>
                </div>
                <div>
                    <h3 class="text-lg font-semibold mb-4 text-gray-700">Panel Details</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <input name="bgcolor" type="text" placeholder="Background Color"
                            class="border p-3 rounded w-full bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400">
                        <input name="panelcolor" type="text" placeholder="Panel Color"
                            class="border p-3 rounded w-full bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400">
                        <input name="textcolor" type="text" placeholder="Text Color"
                            class="border p-3 rounded w-full bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-400">
                    </div>
                </div>
                <button class="w-full py-3 bg-gradient-to-r from-blue-500 to-blue-700 text-white font-semibold rounded-full hover:from-blue-600 hover:to-blue-800 shadow-lg transition flex items-center justify-center">
                    <svg class="h-5 w-5 mr-2" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" /></svg>
                    Create New Product
                </button>
            </form>
        </main>
    </div>
</div>
<%- include('./partials/footer') %>    
</body>

</html>